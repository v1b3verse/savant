# Docker compose integration

* Docker compose with home assistant (where Savant is added as integration) is running at /opt/docker/ha.yml.
* Keep Docker up to date, if you change anything in the sources, restart compose.
